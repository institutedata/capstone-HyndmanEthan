// Desc: HomeCardContainer component for the vendor home page
import { Grid } from '@mui/material';
import VendorStamps from './VendorStamps';



const HomeCardContainer = () => {
  
  return (
    <Grid xs={4}>

    </Grid>
  );
};

export default HomeCardContainer;